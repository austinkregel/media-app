<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-grey-lightest h-screen antialiased">
<div class="flex flex-col">
    <div class="min-h-screen flex items-center justify-center">
        <div class="flex flex-col justify-around h-full">
            <div class="flex justify-between">
                @if(user)
                    @if(route('path').includes('/home'))
                        <a class="hover:bg-blue hover:text-white border border-blue rounded py-1 px-2 text-blue-dark no-underline" href="{{ url('/home') }}">Home</a>
                    @endif
                @else
                    <a class="hover:bg-blue hover:text-white border border-blue rounded py-1 px-2 text-blue-dark no-underline" href="{{ url('/login') }}">Login</a>

                    @if(route('path').includes('/register'))
                        <a class="hover:bg-blue hover:text-white border border-blue rounded py-1 px-2 text-blue-dark no-underline" href="{{ url('/register') }}">Register</a>
                    @endif
                @endif

            </div>
            <div>
                <h1 class="text-grey-darker text-center font-thin tracking-wide text-5xl mb-6">
                    FringeJS
                </h1>
                @if(user)
                    <div class="text-grey-darker text-center font-thin tracking-wide text-lg mb-6">
                        Hi {{user.name}}
                    </div>
                @endif
            </div>
        </div>
    </div>
</div>
</body>
</html>
